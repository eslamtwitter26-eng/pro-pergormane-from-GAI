{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "npm run dev --workspace=artifacts/trading-dashboard",
    "build": "npm run build --workspace=artifacts/trading-dashboard",
    "typecheck": "npm run typecheck --workspace=artifacts/trading-dashboard",
    "build:preview": "npm run build:preview --workspace=artifacts/trading-dashboard"
  },
  "private": true,
  "workspaces": [
    "artifacts/*",
    "lib/*",
    "scripts"
  ],
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  },
  "dependencies": {
    "html2canvas": "^1.4.1"
  }
}
